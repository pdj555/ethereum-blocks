{
  "name": "Ethereum Block Explorer",
  "user": "ubuntu",
  "install": "npm ci && npx playwright install --with-deps chromium && (cd web && npm ci)",
  "terminals": [
    {
      "name": "web-dev",
      "command": "cd web && npm run dev",
      "description": "Next.js dev server for the browser explorer at http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "web", "port": 3000 }
  ]
}
